<Window xmlns:UserControls="clr-namespace:MobileView_Wpf.Views.UserControls"  x:Class="MobileView_Wpf.Views.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MobileView_Wpf.Views.Windows"
        xmlns:behaviors="clr-namespace:MobileView_Wpf.Infrastructure.Behaviors"
        mc:Ignorable="d"
        Title="MainWindow"
        WindowStyle="None"
        Height="632" 
        Width="337"
        behaviors:AspectRatio.Ratio="0.5625">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        
        <UserControls:TitleBar 
            Grid.Row="0" 
            TextValue="{Binding SharedText, Mode=TwoWay}"/>

        <Grid Grid.Row="1" Background="AliceBlue">
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock 
                Text="{Binding SharedText, Mode=TwoWay}"
                Grid.Row="0"
                FontSize="14"
                Margin="5"/>
        </Grid>

    </Grid>
</Window>
